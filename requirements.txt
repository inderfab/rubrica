fastapi==0.115.5
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.12
pyyaml==6.0.2
aiofiles==24.1.0
httpx==0.28.0          # für FastAPI TestClient
pytest==8.3.4
pytest-asyncio==0.24.0

# vCard-Import (Phase 0)
vobject==0.9.7

# CardDAV-Server (Phase 2)
radicale==3.7.6
bcrypt==4.2.1           # fuer htpasswd-Auth (radicale.auth.htpasswd)

# Export (Phase 3)
reportlab==5.0.0        # PDF-Erzeugung
